language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t lafountain/docker-react -f client/Dockerfile.dev .

script:
  - docker run -e lafountain/docker-react npm run test
